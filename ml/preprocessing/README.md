# ML Preprocessing

This directory will contain reproducible preprocessing pipelines for vision and text data:

- **Vision**: Resize, normalize, color space transforms, and quality estimation (blur detection, exposure checks).
- **Text**: Lowercasing, punctuation stripping, tokenization, stop-word handling, and optional lemmatization.

*Preprocessing scripts will be added when training pipelines begin.*
