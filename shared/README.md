# Shared Schemas & Contracts

This directory contains versioned contracts and schemas shared between the client applications (Mobile / Dashboard) and the Backend API.

## Schemas

- [`schemas/report_submission.json`](./schemas/report_submission.json): The canonical JSON Schema for report submissions from client edge devices to the ingestion API (`v1.0.0`).
